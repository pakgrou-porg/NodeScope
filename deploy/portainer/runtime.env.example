# Create /srv/nodescope/runtime/runtime.env on Framework as root with mode 0600.
# This file is mounted read-only into the container and is never committed.
NODESCOPE_RUNTIME_DB_PASSWORD=REPLACE_WITH_NODESCOPE_RUNTIME_LOGIN_PASSWORD
